import React from "react";

import { SecondStageText } from "./style";

const SecondStage = (): React.JSX.Element => {
  return <SecondStageText>You don`t have the second stage, so just skip this step</SecondStageText>;
};

export default SecondStage;
