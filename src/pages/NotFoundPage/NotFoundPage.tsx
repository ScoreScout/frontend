import React from "react";
import { StyledNotFound } from "./stlye";

const NotFoundPage = (): React.JSX.Element => {
  return <StyledNotFound>OOPS! page not found</StyledNotFound>;
};

export default NotFoundPage;
